#include <stdio.h>
#include <string>
namespace rpu_esl_model
{
	//int ch16toi(char ch)
	//{
	//	if (ch >= '0'&&ch <= '9')
	//		return ch - '0';

	//	if (ch >= 'a'&&ch <= 'f')
	//		return ch - 'a' + 10;

	//	if (ch >= 'A'&&ch <= 'F')
	//		return ch - 'A' + 10;
	//}

	//long hex2dec(char* str)
	//{
	//	char *p;
	//	long x = 0;

	//	for (p = str; *p != '\0'; p++)
	//	{
	//		x = x * 16 + ch16toi(*p);
	//	}
	//	return x;
	//}
}